# Mirrors workspace/inventory/hosts.yml for Molecule's own view of the fleet.
all:
  children:
    fleet:
      hosts:
        sdc-web:
          ansible_host: localhost
          ansible_port: 2221
        sdc-db:
          ansible_host: localhost
          ansible_port: 2222
        sdc-comms:
          ansible_host: localhost
          ansible_port: 2223
